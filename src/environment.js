export const environment = {
    URL: window.location.protocol + '//' + window.location.host,
    API:'http://ecommerce-api.test/api/',
    // URL: window.location.protocol + '//' + window.location.hostname + window.location.pathname.slice(0,-1),
};